data Hoge

greet : Hoge -> String =
    | _ => "Hogeeeeee"

data Fuga

greet : Fuga -> String =
    | _ => "Fugaaaaaa"

main : () -> () =
    | () => do
        println(greet(Hoge))
        println(greet(Fuga))
