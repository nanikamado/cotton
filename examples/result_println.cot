main : () -> () =
    | () => do
        ((do
            Ok(Ok("ok")? ++ Ok("!")?)
        ) : Result[String, String]).println
        (do
            Ok(Ok("ok")? ++ Err("!")?)
        ).println
        ()
