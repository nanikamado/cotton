add_one : (I64 | String) -> (I64 | String) =
    | a : I64 => a + 1
    | a : String => a <> " one"

to_string : (I64 | String) -> String =
    | a : I64 => a.i64_to_string
    | a : String => a

main : () -> () =
    | () => do
        1.add_one.to_string.println_str
        "1".add_one.to_string.println_str
        ()
